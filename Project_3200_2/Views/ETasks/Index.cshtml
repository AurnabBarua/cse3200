@model IEnumerable<Project_3200_2.Models.ETask>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout_Admin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn button" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ETaskName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ETaskStartTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ETaskEndTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ETaskName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ETaskStartTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ETaskEndTime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ETaskId }, new { @class = "btn button" }) |
            @Html.ActionLink("Details", "Details", new { id=item.ETaskId }, new { @class = "btn button" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ETaskId }, new { @class = "btn button" })
        </td>
    </tr>
}

</table>
