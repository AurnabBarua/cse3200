@model IEnumerable<Project_3200_2.Models.Prescription>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout_Admin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn button" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Doctor.DoctorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patient.PatientName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PrescriptionMessage)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Doctor.DoctorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.PatientName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PrescriptionMessage)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PrescriptionId }, new { @class = "btn button" }) |
            @Html.ActionLink("Details", "Details", new { id=item.PrescriptionId }, new { @class = "btn button" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PrescriptionId }, new { @class = "btn button" })
        </td>
    </tr>
}

</table>
